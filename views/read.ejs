<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Read Items</title>
    <style>
        /* 全局样式 */
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            min-height: 100vh;
            background: linear-gradient(to bottom right, #4facfe, #00f2fe);
            color: #333;
        }

        /* 主容器样式 */
        .container {
            display: flex;
            background-color: #fff;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            max-width: 900px;
            width: 90%;
            margin-top: 30px;
        }

        /* 左侧列表样式 */
        .item-list {
            width: 40%;
            margin-right: 20px;
            border-right: 2px solid #e0e0e0;
            padding-right: 20px;
        }

        .item-list h1 {
            font-size: 1.5rem;
            margin-bottom: 15px;
            color: #007BFF;
            text-align: center;
        }

        .item-list ul {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        .item-list li {
            padding: 12px 15px;
            cursor: pointer;
            border-bottom: 1px solid #e0e0e0;
            transition: all 0.3s ease;
            font-size: 1rem;
            border-radius: 8px;
        }

        .item-list li:hover {
            background-color: #f0f8ff;
            color: #007BFF;
            transform: translateX(5px);
        }

        /* 右侧详情样式 */
        .item-details {
            width: 60%;
            padding-left: 20px;
        }

        .item-details h2 {
            font-size: 1.8rem;
            color: #333;
            margin-bottom: 15px;
            text-align: center;
        }

        .item-details span {
            display: block;
            margin-bottom: 15px;
            font-size: 1.1rem;
            color: #555;
        }

        .item-details span strong {
            color: #007BFF;
        }

        /* 响应式设计 */
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
                align-items: center;
            }

            .item-list {
                width: 100%;
                margin-right: 0;
                border-right: none;
                padding-right: 0;
                border-bottom: 2px solid #e0e0e0;
                padding-bottom: 20px;
            }

            .item-details {
                width: 100%;
                padding-left: 0;
                margin-top: 20px;
            }
        }
    </style>
</head>
<body>

<div class="container">
    <!-- 左侧：显示所有 items 名称 -->
    <div class="item-list">
        <h1>Items List</h1>
        <ul id="itemsList">
            <% items.forEach(function(item) { %>
                <li onclick="showItemDetails('<%= item.name %>', <%= item.count %>, <%= item.singlePrice %>)">
                    <%= item.name %>
                </li>
            <% }) %>
        </ul>
    </div>

    <!-- 右侧：显示点击后的 item 详情 -->
    <div class="item-details">
        <h2>Item Details</h2>
        <span><strong>Name:</strong> <span id="detailName">Click an item to see details</span></span>
        <span><strong>Count:</strong> <span id="detailCount">-</span></span>
        <span><strong>Single Price:</strong> <span id="detailPrice">-</span></span>
    </div>
</div>

<script>
    // JavaScript 函数，用于显示点击的 item 的详情
    function showItemDetails(name, count, singlePrice) {
        document.getElementById('detailName').textContent = name;
        document.getElementById('detailCount').textContent = count;
        document.getElementById('detailPrice').textContent = '$' + singlePrice.toFixed(2);
    }
</script>

</body>
</html>